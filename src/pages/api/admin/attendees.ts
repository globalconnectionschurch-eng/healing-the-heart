import type { APIRoute } from 'astro';
import { env } from 'cloudflare:workers';
import { isAdminRequest, json, nowIso } from '../../../lib/server';
export const prerender=false;
export const GET: APIRoute=async({request,url})=>{if(!(await isAdminRequest(request)))return json({error:'Unauthorized'},401);const q=`%${(url.searchParams.get('q')||'').trim()}%`;const {results}=await env.DB.prepare(`SELECT s.*,COUNT(r.id) registration_count,MAX(r.registered_at) last_registered_at FROM students s LEFT JOIN registrations r ON r.student_id=s.id WHERE s.name LIKE ? OR s.email LIKE ? OR COALESCE(s.phone,'') LIKE ? GROUP BY s.id ORDER BY s.name COLLATE NOCASE LIMIT 500`).bind(q,q,q).all<any>();return json({students:results});};
export const PUT: APIRoute=async({request})=>{if(!(await isAdminRequest(request)))return json({error:'Unauthorized'},401);const d=await request.json() as Record<string,any>;if(!d.id)return json({error:'Student ID is required.'},400);await env.DB.prepare(`UPDATE students SET name=?,email=?,phone=?,address=?,date_of_birth=?,marital_status=?,church=?,sr_pastor=?,how_heard=?,goals=?,smoking_drinking=?,anything_else=?,mass_email_opt_out=?,updated_at=? WHERE id=?`).bind(d.name||'',d.email||'',d.phone||'',d.address||'',d.date_of_birth||'',d.marital_status||'',d.church||'',d.sr_pastor||'',d.how_heard||'',d.goals||'',d.smoking_drinking||'',d.anything_else||0,d.mass_email_opt_out?1:0,nowIso(),d.id).run();return json({ok:true});};
